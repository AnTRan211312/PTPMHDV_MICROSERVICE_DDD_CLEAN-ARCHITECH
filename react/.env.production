# =============================================================================
# PRODUCTION ENVIRONMENT
# =============================================================================
# These values are injected at Docker build time via --build-arg
# Or you can replace these placeholders manually before building

# API Gateway Base URL - Default to K8s NodePort service
# Change this to your actual production API URL
VITE_API_BASE_URL=http://localhost:30080/api

# Infrastructure URLs (for admin monitoring dashboard)
# In K8s, these services may be exposed via NodePort or Ingress
VITE_PROMETHEUS_URL=http://localhost:30090
VITE_GRAFANA_URL=http://localhost:30300
VITE_EUREKA_URL=http://localhost:30761
VITE_KAFKA_UI_URL=http://localhost:30888
